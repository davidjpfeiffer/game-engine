// #ifndef __GAME
// #define __GAME

// #include "player.h"

// class Game
// {
// public:

  // virtual bool isOver(const Board & board) = 0;
  
  // virtual bool isValidMove(const Board & board, const Board & boardAfterMove, Player<Game, Board, BoardValue> player) = 0;
  
  // virtual Board getEmptyBoard() = 0;
  
  // virtual Board getCopyOfBoard(const Board & board) = 0;
  
  // virtual void printBoard() = 0;
// };

// #endif